#!/usr/bin/python3
def square_matrix_simple(matrix[]):
    for row in range(len(matrix)):
        for column in (len(matrix[0])):
            square_matrix = (matrix[row][column]) * (matrix[row][column])
    return square_matrix
